import React from 'react';

const Header = () => {
  return (
    <div>
      <h1>Fluid Type Scale Calculator</h1>
      <h3>
        Generate font size variables for a fluid type scale with CSS clamp. Grab
        the output CSS and drop it into any design system.
      </h3>
    </div>
  );
};

export default Header;
